/** Normalize text for accent-insensitive search: lowercase, strip diacritics, trim */
export function normalizeText(text: string): string {
  return text
    .toLowerCase()
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .trim();
}
